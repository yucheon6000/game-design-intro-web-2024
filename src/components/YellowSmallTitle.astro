---
interface Props {
    textAline?: string;
}

const { textAline } = Astro.props;
---

<p class="no-drag"><slot /></p>
<style define:vars={{ textAline }}>
    p {
        font-size: 1rem;
        font-weight: 600;
        color: #ffbd28;
        margin-bottom: 1rem;
        text-align: var(--textAline);
    }
</style>
